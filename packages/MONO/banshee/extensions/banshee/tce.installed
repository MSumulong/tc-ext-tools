#!/bin/sh

[ $(which gtk-update-icon-cache) ] && gtk-update-icon-cache -q -f -t /usr/local/share/icons/hicolor

# locales fix
[ -d /tmp/tcloop/banshee ] && cp -a /tmp/tcloop/banshee/usr/local/bin/* /usr/local/bin

