#!/bin/sh

if [ `which install-info` ]
then
  infodir=/usr/local/share/info
  infos="libunistring.info"

  for info in $infos
  do
    install-info $infodir/$info $infodir/dir > /dev/null 2>&1
  done
fi

