--- a/src/nm-policy-hosts.c	2011-04-19 05:06:22.000000000 +0000
+++ b/src/nm-policy-hosts.c	2011-10-10 12:51:48.735205075 +0000
@@ -66,8 +66,8 @@
 	GError *error = NULL;
 	GString *new;
 
-	if (!g_file_get_contents (SYSCONFDIR "/hosts", &contents, &contents_len, &error)) {
-		nm_log_warn (LOGD_DNS, "couldn't read " SYSCONFDIR "/hosts: (%d) %s",
+	if (!g_file_get_contents ("/etc/hosts", &contents, &contents_len, &error)) {
+		nm_log_warn (LOGD_DNS, "couldn't read /etc/hosts: (%d) %s",
 		             error ? error->code : 0,
 		             (error && error->message) ? error->message : "(unknown)");
 		g_clear_error (&error);
@@ -79,8 +79,8 @@
 		nm_log_info (LOGD_DNS, "Cleaning leftovers from /etc/hosts");
 
 		g_clear_error (&error);
-		if (!g_file_set_contents (SYSCONFDIR "/hosts", new->str, -1, &error)) {
-			nm_log_dbg (LOGD_DNS, "couldn't update " SYSCONFDIR "/hosts: (%d) %s",
+		if (!g_file_set_contents ("/etc/hosts", new->str, -1, &error)) {
+			nm_log_dbg (LOGD_DNS, "couldn't update /etc/hosts: (%d) %s",
 			            error ? error->code : 0,
 			            (error && error->message) ? error->message : "(unknown)");
 			g_clear_error (&error);
