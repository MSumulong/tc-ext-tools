COMMENTS="In case u use the wm shutdown/restart instead of the exit 
		tc gui the backup will be performed depending on the global
		tc BACKUP variable.
		optional dep: 
		   -ephysics for the physics module.
		   -e-modules-extra
		   -evas_generica_loaders
		   -egueb or evas_generic_loaders+librsvg for svg
		    (first way doesn't work for me)
		For a cannection manager efl based u can try econnman"
