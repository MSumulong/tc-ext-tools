#!/bin/sh

THIS=$(basename $0)

if [ -d "/tmp/tcloop/$THIS" ]
then
     for i in $(ls /tmp/tcloop/$THIS/usr/local/lib/*)
     do
          ln -sf $i ${i#/tmp/tcloop/$THIS}
     done

     ln -sf "/tmp/tcloop/$THIS/usr/local/include/GL/glut.h" /usr/local/include/GL/glut.h
fi

