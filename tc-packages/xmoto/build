#
# tc-ext-tools build functions: configure, compile, install, rules
#

# Configure
build_configure() {

   ./configure --prefix=/usr/local --disable-sdltest \
        --with-internal-xdg=1 || return 1

}

# Compile
build_compile() {

   make || return 1

}

# Install
build_install() {

   make DESTDIR="$INSTALL_ROOT" mangdir=/usr/local/share/man/man6 install || return 1

   install -Dm644 extra/xmoto.desktop $INSTALL_ROOT/usr/local/share/applications/xmoto.desktop || return 1
   install -Dm644 extra/xmoto.xpm $INSTALL_ROOT/usr/local/share/pixmaps/xmoto.xpm || return 1

}

# Rules
build_rules() {

   tet_load
   tet_download
   tet_convert
   tet_unpack
   tet_patch
   tet_configure
   tet_compile
   tet_install
   tet_split
   tet_strip
   tet_create
   tet_generate
   tet_check
   tet_package

}
