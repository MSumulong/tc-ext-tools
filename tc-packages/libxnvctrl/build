#
# tc-ext-tools build functions: configure, compile, install, rules
#

# Configure
build_configure() {

   return 0

}

# Compile
build_compile() {

   cd src/libXNVCtrl

   make clean
   make || return 1

}

# Install
build_install() {

   cd src/libXNVCtrl

   install -Dm644 libXNVCtrl.a ${INSTALL_ROOT}/usr/local/lib/libXNVCtrl.a || return 1
   install -Dm644 NVCtrl.h ${INSTALL_ROOT}/usr/local/include/NVCtrl/NVCtrl.h || return 1
   install -Dm644 NVCtrlLib.h ${INSTALL_ROOT}/usr/local/include/NVCtrl/NVCtrlLib.h || return 1
   install -Dm644 nv_control.h ${INSTALL_ROOT}/usr/local/include/NVCtrl/nv_control.h || return 1

}

# Rules
build_rules() {

   tet_load
   tet_download
   tet_convert
   tet_unpack
   tet_patch
   tet_configure
   tet_compile
   tet_install
   tet_split
   tet_strip
   tet_create
   tet_generate
   tet_check
   tet_package

}
